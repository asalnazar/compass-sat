// Free mode — paywall is intentionally disabled. Component kept as a no-op so
// existing imports don't break.
export default function PaywallBanner(_: { title?: string; subtitle?: string; cta?: string } = {}) {
  return null;
}
